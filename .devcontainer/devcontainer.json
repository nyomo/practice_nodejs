{
    "name": "Node.js Dev Container",
    "dockerComposeFile": "./docker-compose.yml",
    "service": "devcontainer",
    "workspaceFolder": "/workdir",
    "postCreateCommand": "apt-get install -y --no-install-recommends gh curl ssh && git config --global --add safe.directory /workspace" 
}